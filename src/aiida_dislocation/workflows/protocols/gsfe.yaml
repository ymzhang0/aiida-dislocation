metadata:
    options:
        withmpi: True

parameters:
    CONTROL:
        calculation: scf
    SYSTEM:
        nosym: False
        noinv: False

uc:
    pw:
        metadata: *metadata
        parameters: *parameters

sf:
    pw:
        metadata: *metadata
        parameters: *parameters

surface:
    pw:
        metadata: *metadata
        parameters: *parameters
